<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="dialog_anim_from_bottom">
        <item name="android:windowEnterAnimation">@anim/dialog_anim_from_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_anim_from_bottom_out</item>
    </style>

    <style name="dialog_anim_scale">
        <item name="android:windowEnterAnimation">@anim/dialog_anim_scale_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_anim_scale_out</item>
    </style>

</resources>
